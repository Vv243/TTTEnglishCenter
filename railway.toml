[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/stats/summary/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[[services]]
name = "backend"
rootDirectory = "backend"
